rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // INSECURE: anyone with the path can read.
      allow read: if true;
      // INSECURE: any authenticated user can write anywhere.
      allow write: if request.auth != null;
    }
  }
}
